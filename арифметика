#include <stdio.h>

int main() { 
	
	int a, b;

	if (scanf("%d %d", &a, &b) != 2) {
		printf("erorr");
		return 0;
	}

	int sum=a+b;
	int diff=a-b;
	int prod=a*b;
	int quot=a/b;

	printf("%d %d %d %d", sum, diff, prod, quot	);

	return 0;
}
